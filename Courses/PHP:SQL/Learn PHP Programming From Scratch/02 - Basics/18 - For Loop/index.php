<?php
    // $numA will start with the value of 1 and the block will run if $numA is less or equal to 10, then we increment $numA
    for($numA = 1; $numA <=10; $numA++){ // So, starting value, condition, and incrementation
        echo 'This<br>';
    }
    // // $numB will start with the value of 2 and the block will run if $numB is less or equal to 20, then we increment $numB by adding 2
    for($numB = 2; $numB <= 20; $numB = $numB + 2){
        echo 'That<br>';
    }
?>